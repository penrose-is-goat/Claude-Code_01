{
  "name": "claude-code-01",
  "private": true,
  "description": "Repository root. The app lives in zillow-tracker/ — these scripts forward to it.",
  "scripts": {
    "go": "npm --prefix zillow-tracker run go",
    "harvest": "npm --prefix zillow-tracker run harvest --",
    "verify-zillow": "npm --prefix zillow-tracker run verify-zillow --",
    "verify-ui": "npm --prefix zillow-tracker run verify-ui",
    "poll": "npm --prefix zillow-tracker run poll",
    "dev": "npm --prefix zillow-tracker run dev",
    "test": "npm --prefix zillow-tracker test",
    "install-app": "npm --prefix zillow-tracker install && npm --prefix zillow-tracker exec prisma generate"
  }
}
